#include <thread>
#include <vector>
#include <iostream>


using namespace std;


/*
vector<int> g_v{1, 2, 3};
void myprint(){

	cout << "thread id : " << std::this_thread::get_id() << ", " << g_v[0] << " " << g_v[1] << " " << g_v[2] << endl;
}
*/

int main(){

	/*
	vector<thread> ths;

	for(int i = 0; i < 3; ++i){

		ths.push_back(thread(myprint));
	}


	for(int i = 0; i < 3; ++i){
		ths[i].join();
	}
	cout << " i love china " << std::endl;
	*/
	return 0;
}
